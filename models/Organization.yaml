title: Organization
description: Describes the purpose, history, function and relation to other organizations over time.
type: object
properties:
  administrativeHistoryNote:
    $ref: properties/administrativeHistoryNote.yaml

  authorityType:
    $ref: properties/authorityType.yaml

  jurisdictions:
    type: array
    items:
      $ref: properties/jurisdiction.yaml

  heading:
    $ref: properties/heading.yaml

  linkCounts:
    $ref: properties/orgLinkCounts.yaml

  naId:
    $ref: properties/naId.yaml

  organizationNames:
    type: array
    items:
      $ref: properties/organizationName.yaml

  personalReferences:
    type: array
    items:
      $ref: properties/authorityReference.yaml

  programAreas:
    type: array
    items:
      $ref: properties/programArea.yaml

  sourceNotes:
    type: array
    items:
      $ref: properties/sourceNote.yaml
