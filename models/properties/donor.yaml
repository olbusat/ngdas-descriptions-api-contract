$schema: http://json-schema.org/draft-07/schema#

title: donor
description: >-
  The name of an organization or person that has donated a collection of archival materials.
type: object
properties:
  authorityType:
    $ref: contextualAuthorityType.yaml

  heading:
    $ref: heading.yaml

  naId:
    $ref: naId.yaml
