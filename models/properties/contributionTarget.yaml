$schema: http://json-schema.org/draft-07/schema#

title: contributionTarget
description: >-
  Values needed to identify the description, authority, or digital object that is the target
  of this contribution.
type: object
properties:
  naId:
    $ref: naId

  objectId:
    $ref: objectId

  pageNum:
    $ref: pageNum
