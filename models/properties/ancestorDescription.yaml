$schema: http://json-schema.org/draft-07/schema#

title: ancestorDescription
description: Base model for ancestors (parent, grandparent, etc..) to descriptions
type: object
properties:
  distance:
    $ref: distance.yaml

  inclusiveEndDate:
    $ref: inclusiveEndDate.yaml

  inclusiveStartDate:
    $ref: inclusiveStartDate.yaml

  levelOfDescription:
    $ref: levelOfDescription.yaml

  naId:
    $ref: naId.yaml

  title:
    $ref: title.yaml
