title: authorityReference
description: >-
  Heading and identifier of a primary authority record.  Represents a relationship between
  authorities or between authorities and descriptions.

type: object
properties:
  authorityType:
    $ref: authorityType.yaml

  heading:
    $ref: heading.yaml

  naId:
    $ref: naId.yaml
