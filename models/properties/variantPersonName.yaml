title: variantPersonName
description: >-
  Additional or variant names commonly used for the person, including nicknames, 
  pen names, or transliterations.
type: object
properties:
  birthDate:
    $ref: birthDate.yaml

  deathDate:
    $ref: deathDate.yaml

  fullerFormOfName:
    $ref: fullerFormOfName.yaml

  heading:
    $ref: heading.yaml

  name:
    $ref: personName.yaml

  numerator:
    $ref: numerator.yaml

  personalTitle:
    $ref: personalTitle.yaml
