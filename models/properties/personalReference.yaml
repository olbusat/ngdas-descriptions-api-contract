title: personalReference
description: The name of an individual who is the subject of or highly relevant to the archival materials.
type: object
properties:
  authorityType:
    $ref: contextualAuthorityType.yaml

  birthDate:
    $ref: birthDate.yaml

  deathDate:
    $ref: deathDate.yaml
required:
  - authorityType
  - birthDate