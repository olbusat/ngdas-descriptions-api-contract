title: creatingOrganization
description: >-
  The name of an organization responsible for the creation, accumulation, or maintenance of the
  series when in working (primary) use.
type: object
allOf:
  - $ref: creator.yaml
properties:
  abolishDate:
    $ref: abolishDate.yaml

  establishDate:
    $ref: establishDate.yaml
required:
  - authorityType
  - establishDate