title: contributor
description: >-
  An organization or person, other than the archival creator, responsible for the
  intellectual, technical, artistic, or financial production of the archival materials.
type: object
properties:
  authorityType:
    $ref: contextualAuthorityType.yaml

  contributorType:
    $ref: contributorType.yaml

  heading:
    $ref: heading.yaml

  naId:
    $ref: naId.yaml
required:
  - naId
  - contributorType
  - authorityType