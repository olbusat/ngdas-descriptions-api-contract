$schema: http://json-schema.org/draft-07/schema#

title: Person
description: Person model description
type: object
properties:
  authorityType:
    $ref: properties/authorityType.yaml

  biographicalNote:
    $ref: properties/biographicalNote.yaml

  birthDate:
    $ref: properties/birthDate.yaml

  deathDate:
    $ref: properties/deathDate.yaml

  fullerFormOfName:
    $ref: properties/fullerFormOfName.yaml

  heading:
    $ref: properties/heading.yaml

  importRecordControlNumber:
    $ref: properties/importRecordControlNumber.yaml

  linkCounts:
    $ref: properties/personLinkCounts.yaml

  naId:
    $ref: properties/naId.yaml

  name:
    $ref: properties/personName.yaml

  numerator:
    $ref: properties/numerator.yaml

  organizationalReferences:
    $ref: properties/authorityReference.yaml

  personalTitle:
    $ref: properties/personalTitle.yaml

  recordSource:
    $ref: properties/recordSource.yaml

  role:
    $ref: properties/role.yaml

  sourceNotes:
    type: array
    items:
      $ref: properties/sourceNote.yaml

  variantPersonNames:
    type: array
    items:
      $ref: properties/variantPersonName.yaml
