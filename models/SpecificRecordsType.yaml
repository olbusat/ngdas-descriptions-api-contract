$schema: http://json-schema.org/draft-07/schema#

title: SpecificRecordsType
description: Describes the intellectual format of the archival materials.
type: object
properties:
  authorityType:
    $ref: properties/authorityType.yaml

  broaderTerms:
    type: array
    items:
      $ref: properties/broaderTerm.yaml

  heading:
    $ref: properties/heading.yaml

  importRecordControlNumber:
    $ref: properties/importRecordControlNumber.yaml

  linkCounts:
    $ref: properties/specificRecordsTypeLinkCounts.yaml

  naId:
    $ref: properties/naId.yaml

  narrowerTerms:
    type: array
    items:
      $ref: properties/narrowerTerm.yaml

  recordSource:
    $ref: properties/recordSource.yaml

  relatedTerms:
    type: array
    items:
      $ref: properties/relatedTerm.yaml

  scopeNote:
    $ref: properties/scopeNote.yaml

  sourceNotes:
    type: array
    items:
      $ref: properties/sourceNote.yaml

  useFor:
    type: array
    items:
      $ref: properties/useFor.yaml
