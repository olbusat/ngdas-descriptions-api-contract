$schema: http://json-schema.org/draft-07/schema#

title: TopicalSubject
description: Describes the topics represented in archival materials.
type: object
properties:
  authorityType:
    $ref: properties/authorityType

  broaderTerms:
    type: array
    items:
      $ref: properties/broaderTerm

  heading:
    $ref: properties/heading

  linkCounts:
    $ref: properties/topicalSubjectLinkCounts

  naId:
    $ref: properties/naId

  narrowerTerms:
    type: array
    items:
      $ref: properties/narrowerTerm

  relatedTerms:
    type: array
    items:
      $ref: properties/relatedTerm

  recordType:
    $ref: properties/recordType

  recordSource:
    $ref: properties/recordSource

  scopeNote:
    $ref: properties/scopeNote

  sourceNotes:
    type: array
    items:
      $ref: properties/sourceNote

  useFor:
    type: array
    items:
      $ref: properties/useFor
