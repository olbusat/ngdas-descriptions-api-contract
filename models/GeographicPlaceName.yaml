$schema: http://json-schema.org/draft-07/schema#

title: GeographicPlaceName
description: Describes the places represented in archival materials.
type: object
properties:
  authorityType:
    $ref: properties/authorityType.yaml

  broaderTerms:
    type: array
    items:
      $ref: properties/broaderTerm.yaml

  coordinates:
    $ref: properties/coordinates.yaml

  heading:
    $ref: properties/heading.yaml

  importRecordControlNumber:
    $ref: properties/importRecordControlNumber.yaml

  linkCounts:
    $ref: properties/geographicPlaceNameLinkCounts.yaml

  naId:
    $ref: properties/naId.yaml

  narrowerTerms:
    type: array
    items:
      $ref: properties/narrowerTerm.yaml

  relatedTerms:
    type: array
    items:
      $ref: properties/relatedTerm.yaml

  recordSource:
    $ref: properties/recordSource.yaml

  recordType:
    $ref: properties/recordType.yaml

  scopeNote:
    $ref: properties/scopeNote.yaml

  sourceNotes:
    type: array
    items:
      $ref: properties/sourceNote.yaml

  useFor:
    type: array
    items:
      $ref: properties/useFor.yaml
